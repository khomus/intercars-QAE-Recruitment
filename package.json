{
  "name": "intercars-qae-recruitment",
  "version": "1.0.0",
  "private": true,
  "description": "QAE test assignment (Playwright + TypeScript)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.7.0"
  }
}
